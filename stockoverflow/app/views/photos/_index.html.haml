.item
  .tag_cloud_div
    - tag_cloud Photo.tag_counts, %w[s m l] do |tag, css_class|
      = link_to "#{tag.name}", tag_path(tag.name), class: css_class

- @photos.each do |photo|
  .item
    .blurtop
      = image_tag photo.image 
    .photo_title
      %h2
        = link_to photo.title, photo
    = image_tag photo.image      
    .question
      %p  
        = truncate photo.question, length: 150
        %br
        Tags: 
        = raw photo.tag_list.map{ |t| link_to t, tag_path(t) }.join(', ') 