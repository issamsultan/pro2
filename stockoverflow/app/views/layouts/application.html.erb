<!DOCTYPE html>
<html>
<head>
  <title>Stockoverflow</title>
  <%= javascript_include_tag "application" %>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <link href='http://fonts.googleapis.com/css?family=Magra' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Inder' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Lato:300,400,700' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Hammersmith+One' rel='stylesheet' type='text/css'>

  <%= csrf_meta_tags %>
</head>

<body>
  <header>
    <div id="header">

      <div id="title">
        <h1>
          <%= link_to 'stock', root_path%><a class="bold">overflow</a>
        </h1>
      </div>

      <div id="links">
        <% if current_user %>
          <div class="button"><%= link_to 'Logout', destroy_user_session_path, method: :delete  %></div>
        <% else %>
          <div class="button"><%= link_to 'Login', user_session_path %></div>
          <div class="button"><%= link_to 'Sign Up', new_user_registration_path, method: :get %></div>
        <% end %>
        <% if current_user %>
          <div class="button"><%= link_to 'Profile', profile_path, method: :get %></div>
          <div class="button"><%= link_to 'Upload', new_photo_path, method: :get %></div>
        <% end %>
      </div>
        <% if current_user %>
          <p class="button" id="hello">You are logged in as <%= current_user.name if current_user %></p>
        <% else %>
          <p class="button" id="hello">Some info</p>
        <% end %>
    </div>
  </header>

  <% if !current_user %>
  <div id="infoban">
    <div id="shad"></div>
    <div id="welcome">
      
      <div id="what"><p>StockOverflow is a question and answer site for professional and enthusiast photographers.<br><br> It's 100% free, no registration required.</p></div>

      <div id="sover"><img src="http://cdn.sstatic.net/Img/sprite-herobox.png"/></div>

      <div id="expcont">
        <div><span>Anybody can ask a question.</span></div>
        <div><span>Anybody can provide<br> an answer.</span></div>
        <div><span>The best answers are<br> voted to the top.</span></div>
      </div>
    </div>
  </div>
  <% end %>

  <div id="page-wrap">

    <div id="centre">
      <%= yield %>
    </div>

  </div>

</body>
</html>
